# Major-tag pinning is deliberate: Dependabot tracks tags, where SHA pinning
# would trade that for a manual bump on every action release.
rules:
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
